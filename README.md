# URL Shortener Service (Django)

Учебный backend‑проект на Django — сервис сокращения ссылок.
Проект принимает длинную ссылку, генерирует короткий код, сохраняет данные в БД и выполняет редирект по короткой ссылке.

Проект сделан с упором на **понимание архитектуры**, работу с **URL routing**, **API**, **БД** и **Git‑workflow**.

---

## 🚀 Возможности

* Создание коротких ссылок через REST API
* Хранение оригинальных URL и short_code в базе данных
* Редирект по короткой ссылке
* SQLite база данных (по умолчанию)
* Чистая архитектура Django‑проекта

---

## 🛠️ Стек технологий

* Python 3.x
* Django 6.0
* SQLite
* Git / GitHub

---

## 📂 Структура проекта

```
url-shortener-service/
├── venv/                # Виртуальное окружение Python
├── src/
│   ├── config/          # Основные настройки Django
│   │   ├── settings.py
│   │   ├── urls.py
│   │   └── wsgi.py
│   ├── links/           # Приложение для сокращения ссылок
│   │   ├── models.py
│   │   ├── views.py
│   │   ├── urls.py
│   │   └── utils.py
│   ├── db.sqlite3       # База данных
│   └── manage.py
└── README.md
```

---

## 🧠 Как работает проект (логика)

1. Пользователь отправляет POST‑запрос с длинным URL
2. Django:

   * принимает запрос
   * генерирует `short_code`
   * сохраняет ссылку в БД
3. Возвращается короткий URL
4. При переходе по короткой ссылке:

   * Django ищет `short_code` в БД
   * выполняет HTTP‑редирект на оригинальный URL

---

## ⚙️ Установка проекта (один раз)

### 1️⃣ Клонировать репозиторий

```bash
git clone https://github.com/USERNAME/url-shortener-service.git
cd url-shortener-service
```

### 2️⃣ Создать виртуальное окружение

```bash
python -m venv venv
```

### 3️⃣ Активировать виртуальное окружение

**Windows (Git Bash):**

```bash
source venv/Scripts/activate
```

После активации появится `(venv)`.

### 4️⃣ Установить зависимости

```bash
pip install -r requirements.txt
```

### 5️⃣ Выполнить миграции

```bash
cd src
python manage.py migrate
```

---

## ▶️ Запуск проекта (каждый раз после перезагрузки ПК)

### 1️⃣ Перейти в папку проекта

```bash
cd ~/Desktop/projects/url-shortener-service/src
```

### 2️⃣ Активировать виртуальное окружение

```bash
source ../venv/Scripts/activate
```

### 3️⃣ Запустить сервер

```bash
python manage.py runserver
```

Сервер будет доступен по адресу:

```
http://127.0.0.1:8000/
```

---

## 🔌 API

### ➕ Создание короткой ссылки

**POST** `/api/shorten/`

**Тело запроса (JSON):**

```json
{
  "url": "https://google.com"
}
```

**Пример curl:**

```bash
curl -X POST http://127.0.0.1:8000/api/shorten/ \
-H "Content-Type: application/json" \
-d "{\"url\": \"https://google.com\"}"
```

**Ответ:**

```json
{
  "short_url": "http://127.0.0.1:8000/AbC123"
}
```

---

### 🔁 Редирект

Открой в браузере:

```
http://127.0.0.1:8000/AbC123/
```

Произойдёт редирект на оригинальный URL.

---

## 🛑 Остановка сервера

В терминале с сервером:

```bash
Ctrl + C
```

---

## 🧠 Важные концепции, которые используются

* Virtual Environment (`venv`)
* Django URL routing
* MVC (MTV) архитектура
* Работа с ORM
* REST API
* HTTP Redirects
* Git commits (conventional commits)

---

## 📌 Возможные улучшения

* Уникальность `short_code`
* Счётчик переходов
* Django REST Framework
* Тесты (pytest / unittest)
* Docker
* Авторизация пользователей

---

## 👨‍💻 Автор

Проект выполнен в учебных целях для изучения backend‑разработки на Django.

---

⭐ Если проект был полезен — поставь звезду репозиторию
